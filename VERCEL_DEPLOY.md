# 🚀 دليل رفع المشروع على Vercel

## الطريقة الأولى: رفع عبر GitHub (الأسهل والأسرع) ⭐

### الخطوة 1: إنشاء مستودع على GitHub

1. اذهب إلى [GitHub](https://github.com)
2. سجل دخول أو أنشئ حساب جديد
3. اضغط على زر **"New"** أو **"+"** في الأعلى
4. اختر **"New repository"**
5. املأ البيانات:
   - **Repository name**: `dhikra` (أو أي اسم تريده)
   - **Description**: "منظم العبادة اليومي - ذِكرىٰ"
   - اختر **Public** أو **Private**
   - **لا** تضع علامة على "Initialize with README"
6. اضغط **"Create repository"**

### الخطوة 2: رفع الملفات إلى GitHub

#### باستخدام GitHub Desktop (الأسهل):

1. حمّل [GitHub Desktop](https://desktop.github.com/)
2. سجل دخول بحسابك
3. اضغط **"File"** → **"Add Local Repository"**
4. اختر مجلد المشروع (`quran`)
5. اكتب رسالة commit مثل: "Initial commit"
6. اضغط **"Commit to main"**
7. اضغط **"Publish repository"**

#### باستخدام Git من Terminal:

```bash
# افتح Terminal في مجلد المشروع
cd C:\Users\pc\Desktop\quran

# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# عمل commit
git commit -m "Initial commit"

# إضافة المستودع البعيد (استبدل YOUR_USERNAME و REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# رفع الملفات
git branch -M main
git push -u origin main
```

### الخطوة 3: رفع المشروع على Vercel

1. اذهب إلى [Vercel](https://vercel.com)
2. اضغط **"Sign Up"** أو **"Log In"**
3. اختر **"Continue with GitHub"**
4. سجل دخول بحساب GitHub
5. اضغط **"Add New..."** → **"Project"**
6. ستجد قائمة بمستودعاتك، اختر المستودع الذي أنشأته
7. اضغط **"Import"**
8. في صفحة الإعدادات:
   - **Project Name**: `dhikra` (أو أي اسم)
   - **Framework Preset**: اختر **"Other"**
   - **Root Directory**: اتركه كما هو (`.`)
   - **Build Command**: اتركه فارغاً
   - **Output Directory**: اتركه فارغاً
9. اضغط **"Deploy"**
10. انتظر دقيقة أو دقيقتين حتى يكتمل الرفع
11. سيظهر لك رابط المشروع مثل: `https://dhikra.vercel.app`

### الخطوة 4: تحديث المشروع

عندما تقوم بتعديل أي ملفات:

1. ارفع التغييرات على GitHub:
   ```bash
   git add .
   git commit -m "Update files"
   git push
   ```

2. Vercel سيقوم تلقائياً بإعادة نشر المشروع! 🎉

---

## الطريقة الثانية: رفع مباشر عبر Vercel CLI

### الخطوة 1: تثبيت Node.js

1. حمّل [Node.js](https://nodejs.org/) (اختر LTS)
2. ثبت البرنامج
3. افتح Terminal/Command Prompt

### الخطوة 2: تثبيت Vercel CLI

```bash
npm install -g vercel
```

### الخطوة 3: رفع المشروع

```bash
# انتقل إلى مجلد المشروع
cd C:\Users\pc\Desktop\quran

# ابدأ عملية الرفع
vercel
```

### الخطوة 4: اتبع التعليمات

1. سيسألك: **"Set up and deploy?"** → اضغط **Y** ثم Enter
2. سيسألك: **"Which scope?"** → اختر حسابك
3. سيسألك: **"Link to existing project?"** → اضغط **N** ثم Enter
4. سيسألك: **"What's your project's name?"** → اكتب `dhikra` أو أي اسم
5. سيسألك: **"In which directory is your code located?"** → اضغط Enter (للبقاء في المجلد الحالي)
6. سيسألك عن الإعدادات → اضغط Enter لكل سؤال (استخدم القيم الافتراضية)
7. انتظر حتى يكتمل الرفع
8. سيظهر لك رابط المشروع!

### للرفع في الإنتاج:

```bash
vercel --prod
```

---

## الطريقة الثالثة: سحب وإفلات (Drag & Drop)

⚠️ **ملاحظة**: هذه الطريقة لا تدعم التحديثات التلقائية

1. اذهب إلى [Vercel](https://vercel.com)
2. سجل دخول
3. اضغط **"Add New..."** → **"Project"**
4. في الأسفل ستجد **"Deploy a static site"**
5. اضغط **"Browse"** أو اسحب مجلد المشروع بالكامل
6. اضغط **"Deploy"**

---

## 🔧 إعدادات إضافية

### إضافة نطاق مخصص (Custom Domain)

1. في Vercel Dashboard، اختر مشروعك
2. اذهب إلى **"Settings"** → **"Domains"**
3. اضغط **"Add Domain"**
4. اكتب النطاق الذي تريده (مثل: `dhikra.com`)
5. اتبع التعليمات لإعداد DNS

### إعدادات البيئة (Environment Variables)

إذا كنت تحتاج متغيرات بيئة:

1. في Vercel Dashboard، اختر مشروعك
2. اذهب إلى **"Settings"** → **"Environment Variables"**
3. اضغط **"Add New"**
4. أدخل الاسم والقيمة
5. اضغط **"Save"**

---

## 📝 ملاحظات مهمة

✅ **الملفات المطلوبة:**
- `index.html`
- `quran.html`
- `dhikr.html`
- `styles.css`
- `script.js`
- `quran.js`
- `dhikr.js`
- `vercel.json` (موجود بالفعل)

✅ **الملفات الاختيارية:**
- `README.md`
- `.gitignore`

❌ **لا تحتاج:**
- `node_modules` (لأن المشروع لا يستخدم npm)
- `package.json` (اختياري)

---

## 🆘 حل المشاكل الشائعة

### المشكلة: الصفحات لا تعمل عند الانتقال بينها

**الحل**: تأكد من وجود ملف `vercel.json` مع الإعدادات الصحيحة (موجود بالفعل)

### المشكلة: الخطوط أو الأيقونات لا تظهر

**الحل**: تأكد من أن جميع الملفات موجودة في نفس المجلد

### المشكلة: API لا يعمل

**الحل**: تأكد من أن API يدعم CORS (API القرآن يدعمه بالفعل)

---

## 🎉 مبروك!

بعد اكتمال الرفع، ستحصل على رابط مثل:
- `https://dhikra.vercel.app`
- أو رابط مخصص إذا أضفت نطاق

**نصيحة**: احفظ الرابط في مكان آمن! 🔖

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع [وثائق Vercel](https://vercel.com/docs)
2. راجع [مستندات GitHub](https://docs.github.com)
3. تأكد من أن جميع الملفات موجودة

---

**تم إنشاء هذا الدليل خصيصاً لمشروع ذِكرىٰ** 🕌
